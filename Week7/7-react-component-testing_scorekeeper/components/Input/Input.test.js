import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import Input from ".";

test("renders a label and an input with the correct attributes", () => {
 /*    render(<Input labelText={'Name of game'} placeholder={"e.g. Dodelido"} name={"nameOfGame"} />);
    
    const label = screen.getByLabelText("Name of game")
    //const input = screen.getByLabelText("Name of game")

    expect(label).toBeInTheDocument();
    expect(label).toHaveAttribute("placeholder", "e.g. Dodelido"); */

});

test("calls callback on every user input", async () => {

});
